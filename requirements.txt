# Core data stack
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=15.0.0
duckdb>=0.10.0

# Excel / reporting
openpyxl>=3.1.0
xlsxwriter>=3.2.0

# Fuzzy matching (entity / reference matching)
rapidfuzz>=3.6.0

# Dates and validation
python-dateutil>=2.9.0
pydantic>=2.6.0
pandera>=0.18.0

# Configuration
pyyaml>=6.0.0
